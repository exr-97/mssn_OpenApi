#Update this config file whenever new service is added
filepaths:
  openAPI:
    reference/service/amion_Interface/amionInterface.yaml
    reference/service/amion_Interface/drm.yaml
  bundledOpenAPI:
    dist/amionInterface.yaml
    dist/drm.yaml

